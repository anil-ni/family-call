// Used only when this page is running inside the native Android app.
// When opened as a normal website (e.g. in Safari on iOS), the app talks
// to whatever host served the page, so this value is ignored.
window.FAMILY_CALL_SERVER = 'https://REPLACE-WITH-YOUR-RENDER-URL.onrender.com';
